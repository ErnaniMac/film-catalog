# Docker user/group IDs - deve corresponder ao seu usuário do host
# Execute 'id' no terminal para obter seus valores
HOST_UID=1000
HOST_GID=1000

# MySQL Database Configuration
MYSQL_DATABASE=film_catalog
MYSQL_ROOT_PASSWORD=root
MYSQL_PASSWORD=password
MYSQL_USER=film_user
MYSQL_PORT=3307

# Laravel Database Configuration (Docker)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=film_catalog
DB_USERNAME=film_user
DB_PASSWORD=password

# Redis Configuration (Docker)
REDIS_HOST=redis
REDIS_PORT=6379

# Service Ports
NGINX_PORT=8000
NODE_PORT=5173
